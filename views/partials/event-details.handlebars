<div class="card h-100" style="width: 18rem;">
    <img src="/gifs/{{image}}" class="card-img-top object-cover" alt="" />
    <div class="card-body">
        <h5 class="card-title">{{name}}</h5>
        <p class="card-text">{{content}}
        </p>
        <p>{{time}}</p>
        <p>${{cost}}</p>
        <a href="/venue/{{id}}" class="btn mc-btn-primary">{{name}}</a>
    </div>
</div>